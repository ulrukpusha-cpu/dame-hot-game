# Copier en .env.local et remplir les valeurs (ne pas commiter .env.local).
# Avec Vite, seules les variables préfixées par VITE_ sont exposées au client (équivalent NEXT_PUBLIC_*).

# ========== API (backend Railway) ==========
VITE_API_URL=https://hot-game-dame-production.up.railway.app
VITE_WS_URL=wss://hot-game-dame-production.up.railway.app

# ========== Telegram ==========
VITE_BOT_USERNAME=DameHotGameBot
# Server only (ne pas préfixer par VITE_) :
# BOT_TOKEN=your_bot_token_here
# TELEGRAM_WEBHOOK_SECRET=your_webhook_secret

# ========== TON ==========
VITE_TON_MANIFEST_URL=https://votre-domaine.com/tonconnect-manifest.json
# Server only :
# TON_CONTRACT_ADDRESS=your_ton_contract_address

# ========== Base URL (production) ==========
# BASE_URL=https://votre-domaine.com

# ========== Base de données (backend Node.js) ==========
# DATABASE_URL=postgresql://user:password@host:5432/database
# REDIS_URL=redis://localhost:6379

# ========== Sécurité (server only) ==========
# JWT_SECRET=your_jwt_secret_here
# ENCRYPTION_KEY=your_encryption_key

# ========== Paiements ==========
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# ========== Monitoring (optionnel) ==========
# SENTRY_DSN=https://...@sentry.io/...
